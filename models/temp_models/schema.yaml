
version: 2

models:
    - name: test_model
      description: "using main table"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
